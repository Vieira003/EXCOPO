package main

import "fmt"

func main() {
    a := 10
    b := 5
    fmt.Println("soma:", soma(a,b))
    fmt.Println("subtrair:", subtrair(a,b))
    fmt.Println("multiplicar:", multiplicar(a,b))
    fmt.Println("dividir:", dividir(a,b))
}